{
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //the following connection will be used to connect directly to the azure sql server (when token authentication via active directory is in use).
    "MyDbConnection": "Server=tcp:enhancedebookdb.database.windows.net,1433;Database=coreDB;"
  }
}